class Solution {
public:
    vector<int> prefSum(vector<int> &arr) {
        int n = arr.size();
        vector<int> sum(n);

        sum[0] = arr[0];

        for (int i = 1; i < n; i++) {
            sum[i] = sum[i - 1] + arr[i];
        }

        return sum;
    }
};
